version: 2
models:
  - name: dim_products
    description: "Dimension model for products, containing enriched category and subcategory details."
    columns:
      - name: product_id
        description: "The unique primary key for each product."
        tests:
          - unique
          - not_null